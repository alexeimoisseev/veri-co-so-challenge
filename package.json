{
  "author": "Zajo at Veri",
  "private": true,
  "name": "so-task-words",
  "version": "0.0.1",
  "description": "Minimal boilerplate for the interview task.",
  "main": "dist/index.js",
  "engines": {
    "node": "^12.20.0 || ^14.13.1 || >=16.0.0"
  },
  "scripts": {
    "build": "tsc",
    "dev": "ts-node-dev --respawn --transpile-only --exit-child --poll src/index.ts",
    "prod": "npm run build && node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^17.0.8",
    "eslint": "^8.6.0",
    "pino-pretty": "^7.3.0",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "fastify": "^3.25.3",
    "got": "11.8.3",
    "postgresql-client": "^1.21.5"
  }
}